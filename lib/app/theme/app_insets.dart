import 'package:flutter/material.dart';

class AppInsets {
  static const extraSmall = EdgeInsets.all(8.0);
  static const small = EdgeInsets.all(12.0);
  static const medium = EdgeInsets.all(16.0);
  static const large = EdgeInsets.all(20.0);
}
